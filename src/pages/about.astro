---
import Layout from "../layouts/Layout.astro";
import { recommendations } from "../data/recommendations";
---

<Layout>
  <article class="px-4 pt-0 pb-16">
    <div
      class="grid grid-cols-1 gap-12 border-t border-dotted border-primary/25 pt-8 md:pt-12 md:grid-cols-2 md:gap-8"
    >
      <!-- Left column: Bio -->

      <div>
        <h2
          class="mb-4 text-xs font-semibold uppercase tracking-widest text-text text-pretty"
        >
          ABOUT
        </h2>
        <div class="flex flex-col gap-6">
          <p class="text-base leading-relaxed text-text">
            I'm a frontend developer from Melbourne, Australia. I am a highly
            curious person who loves trying new things and am currently learning
            creative web technologies like p5.js and three.js.
          </p>
          <p class="text-base leading-relaxed text-text">
            Most of the work here is an attempt to make digital spaces that are
            a little more playful.
          </p>
          <div
            class="profile-wrap mt-2 w-full overflow-hidden border-primary border-3"
          >
            <img
              src="/profile.jpg"
              alt="Hien Nguyen"
              width="600"
              height="600"
              loading="lazy"
              class="profile-img aspect-square w-full object-cover"
            />
          </div>
          <p>
            I enjoy photography, spending time in movie theaters, volunteer at
            the <a
              class="underline hover:text-ink-muted"
              href="https://altonanorthdojo.com/"
              rel="noopener">Altona North Coder Dojo</a
            >. If my work resonates with you, please reach out.
          </p>
          <div class="flex flex-col gap-1 pt-1">
            <div class="text-sm leading-relaxed">
              <span class="font-semibold">Email:</span>
              <a
                href="mailto:hiennguyen1529@gmail.com"
                class="text-text underline decoration-primary/30 underline-offset-[3px] transition-colors duration-150 ease-out hover:decoration-primary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary motion-reduce:transition-none"
                >hiennguyen1529@gmail.com</a
              >
            </div>
            <div class="text-sm leading-relaxed">
              <span class="font-semibold">Arena:</span>
              <a
                href="https://www.are.na/hien-nguyen"
                target="_blank"
                rel="noopener noreferrer"
                class="text-text underline decoration-primary/30 underline-offset-[3px] transition-colors duration-150 ease-out hover:decoration-primary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary motion-reduce:transition-none"
                >are.na/hien<span class="sr-only"> (opens in new tab)</span></a
              >
            </div>
            <div class="text-sm leading-relaxed">
              <span class="font-semibold">Instagram:</span>
              <a
                href="https://www.instagram.com/hiennguyen1529"
                target="_blank"
                rel="noopener noreferrer"
                class="text-text underline decoration-primary/30 underline-offset-[3px] transition-colors duration-150 ease-out hover:decoration-primary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary motion-reduce:transition-none"
                >@hiennguyen1529<span class="sr-only">
                  (opens in new tab)</span
                ></a
              >
            </div>
          </div>
        </div>
      </div>

      <!-- Right column: Recommendations -->
      <div>
        <h2
          class="mb-5 text-xs font-semibold uppercase tracking-widest text-text text-pretty"
        >
          Recommending
        </h2>
        {
          recommendations.length === 0 ? (
            <p class="text-sm text-neutral-500">Nothing here yet.</p>
          ) : (
            <ul role="list" class="flex list-none flex-col p-0">
              {recommendations.map((rec, i) => (
                <li
                  class:list={[
                    i !== recommendations.length - 1 &&
                      "border-b border-primary/8",
                  ]}
                >
                  <a
                    href={rec.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class:list={[
                      "grid grid-cols-[8rem_1fr] items-stretch gap-4 no-underline text-inherit hover:opacity-80 transition-opacity duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-primary rounded-sm motion-reduce:transition-none",
                      i === 0 && "pt-0",
                    ]}
                  >
                    {rec.image ? (
                      <img
                        src={rec.image}
                        alt=""
                        class="w-full aspect-square object-cover"
                        loading="lazy"
                      />
                    ) : (
                      <div
                        class="w-full aspect-square"
                        style={`background-color: ${rec.color}`}
                        aria-hidden="true"
                      />
                    )}
                    <div class="flex min-w-0 flex-col gap-2 py-4">
                      <h3 class="text-base font-semibold leading-tight text-pretty">
                        {rec.title}
                      </h3>
                      <p class="line-clamp-2 text-sm leading-relaxed text-neutral-500">
                        {rec.description}
                      </p>
                      <span class="mt-1 text-xs text-neutral-500">
                        [{rec.category}]
                      </span>
                    </div>
                  </a>
                </li>
              ))}
            </ul>
          )
        }
      </div>
    </div>
  </article>
</Layout>
